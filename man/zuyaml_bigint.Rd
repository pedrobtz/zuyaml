% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bigint.R
\name{zuyaml_bigint}
\alias{zuyaml_bigint}
\title{Integers too large for R's numeric types}
\usage{
zuyaml_bigint(x)
}
\arguments{
\item{x}{A character vector of decimal integers, or an object to coerce.}
}
\value{
\code{zuyaml_bigint()} returns a character vector of class
\code{"zuyaml_bigint"}.
}
\description{
A character vector holding the decimal representation of integers that R
cannot store exactly: beyond 2^53 a double silently loses precision, and R
has no native 64-bit integer scalar.
}
\details{
\code{\link[=yaml_parse]{yaml_parse()}} returns one of these when it meets such a value and
\code{big_integers = "bigint"} (the default). It carries the \emph{decimal}
normalisation of the value, not the source text, so \code{0x1FFFFFFFFFFFFFFF}
and its decimal spelling compare equal.

This is a marker for values whose precision must be preserved, not a
big-integer arithmetic type: there is no arithmetic. Use
\code{as.numeric()} to accept the precision loss deliberately, or a package such
as \code{bit64} for real 64-bit arithmetic.
}
\examples{
yaml_parse("9223372036854775807")
as.numeric(yaml_parse("9223372036854775807")) # lossy, on purpose
}
