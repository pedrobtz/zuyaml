% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emit.R
\name{yaml_emit}
\alias{yaml_emit}
\alias{yaml_emit_all}
\title{Emit YAML}
\usage{
yaml_emit(
  x,
  indent = 2L,
  width = 80L,
  document_start = FALSE,
  document_end = FALSE
)

yaml_emit_all(
  x,
  indent = 2L,
  width = 80L,
  document_start = FALSE,
  document_end = FALSE
)
}
\arguments{
\item{x}{An R object. \code{yaml_emit_all()} takes a list, one element per
document.}

\item{indent}{Spaces per indentation level, 1 to 255.}

\item{width}{Line width before wrapping, 0 to 255. \code{0} disables wrapping.}

\item{document_start}{Emit a leading \verb{---}.}

\item{document_end}{Emit a trailing \code{...}.}
}
\value{
A length-one UTF-8 character vector.
}
\description{
\code{yaml_emit()} converts one R object to a YAML document. \code{yaml_emit_all()}
converts a list of objects to a YAML \emph{stream}.
}
\details{
Emission favours readable YAML over reproducing any particular source
formatting. Comments, quoting style and flow-versus-block choices are not
preserved; see \code{vignette("zuyaml")} for the conversions that do not round
trip.
}
\examples{
cat(yaml_emit(list(host = "localhost", port = 8080L)))
cat(yaml_emit_all(list(list(a = 1L), list(b = 2L))))

# Strings that look like other types stay strings.
cat(yaml_emit(list(version = "42")))
}
