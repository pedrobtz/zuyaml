% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{yaml_parse}
\alias{yaml_parse}
\alias{yaml_parse_all}
\title{Parse YAML}
\usage{
yaml_parse(
  x,
  simplify = FALSE,
  aliases = c("resolve", "error"),
  big_integers = c("bigint", "double", "error"),
  tags = c("ignore", "error"),
  duplicate_keys = FALSE,
  max_depth = 128L,
  max_size = 64 * 1024^2,
  max_nodes = 1e+06,
  path = NULL
)

yaml_parse_all(
  x,
  simplify = FALSE,
  aliases = c("resolve", "error"),
  big_integers = c("bigint", "double", "error"),
  tags = c("ignore", "error"),
  duplicate_keys = FALSE,
  max_depth = 128L,
  max_size = 64 * 1024^2,
  max_nodes = 1e+06,
  path = NULL
)
}
\arguments{
\item{x}{A length-one character vector containing YAML, or a raw vector of
UTF-8 YAML bytes.}

\item{simplify}{If \code{TRUE}, sequences whose elements are all scalars of the
same type collapse to an atomic vector. The default is \code{FALSE}, so the
shape of the result never depends on the contents of the document.}

\item{aliases}{How to treat YAML aliases. \code{"resolve"} (the default) replaces
each alias with the value of its target; node identity is not preserved.
\code{"error"} rejects any document containing an alias.}

\item{big_integers}{How to represent integers too large for an R numeric
type to hold exactly (beyond 2^53). \code{"bigint"} (the default) returns a
\link{zuyaml_bigint} character vector holding the decimal value, \code{"double"} is
an explicit opt-in to lossy conversion, and \code{"error"} refuses the
document. The package never loses integer precision silently.}

\item{tags}{How to treat an application tag such as \code{!duration}. Core
schema tags (\code{!!str}, \code{!!int}, \code{!!float}, \code{!!bool}, \code{!!null}) always
override resolution, so \verb{!!str 12} is the string \code{"12"}. \code{"ignore"} (the
default) converts a tagged value as though it were untagged; \code{"error"}
rejects the document. Ignoring is the default because rejecting would
refuse a great deal of ordinary YAML.}

\item{duplicate_keys}{If \code{FALSE} (the default), a mapping with duplicate
keys is an error. If \code{TRUE}, duplicates become duplicate names in the
resulting list.}

\item{max_depth}{Maximum nesting depth, or \code{0} for unlimited.}

\item{max_size}{Maximum input size in bytes, or \code{0} for unlimited.}

\item{max_nodes}{Maximum number of R values materialised, or \code{0} for
unlimited. This is the limit that bounds alias expansion: a
billion-laughs document is small and shallow, so neither \code{max_size} nor
\code{max_depth} constrains it. The default leaves roughly fifty times the
headroom a very large document needs.}

\item{path}{Optional file path, used only to make error messages more
informative. Set by the file-reading functions in a later milestone.}
}
\value{
\code{yaml_parse()} returns an R object. \code{yaml_parse_all()} returns a
list with one element per document.
}
\description{
\code{yaml_parse()} parses a single YAML document. \code{yaml_parse_all()} parses a
YAML \emph{stream} and returns one element per document.
}
\details{
Both functions parse the input as a stream. \code{yaml_parse()} then requires it
to contain exactly one document, so trailing documents are never silently
discarded:\tabular{lll}{
   documents \tab \code{yaml_parse()} \tab \code{yaml_parse_all()} \cr
   0 \tab error \tab \code{list()} \cr
   1 \tab the object \tab list of length 1 \cr
   more \tab error \tab list of that length \cr
}


A zero-document stream is an error rather than \code{NULL}, because \code{NULL} is
the legitimate result of parsing a document whose content is \code{null}.
Comment-only input is a zero-document stream.
}
\examples{
yaml_parse("null")
yaml_parse_all("---\nnull\n---\nnull\n")
}
