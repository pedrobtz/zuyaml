% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/files.R
\name{yaml_read}
\alias{yaml_read}
\alias{yaml_read_all}
\alias{yaml_write}
\alias{yaml_write_all}
\title{Read and write YAML files}
\usage{
yaml_read(path, ...)

yaml_read_all(path, ...)

yaml_write(x, path, ...)

yaml_write_all(x, path, ...)
}
\arguments{
\item{path}{Path to a file.}

\item{...}{Passed to \code{\link[=yaml_parse]{yaml_parse()}} or \code{\link[=yaml_emit]{yaml_emit()}}.}

\item{x}{An R object. \code{yaml_write_all()} takes a list, one element per
document.}
}
\value{
\code{yaml_read()} returns an R object and \code{yaml_read_all()} a list of
them. The writers return \code{path} invisibly.
}
\description{
\code{yaml_read()} reads one YAML document from a file; \code{yaml_read_all()} reads a
stream. \code{yaml_write()} and \code{yaml_write_all()} are their counterparts.
}
\details{
These work on bytes, not text: files are read and written with \code{\link[=readBin]{readBin()}}
and \code{\link[=writeBin]{writeBin()}}, so nothing depends on the session's locale, and the file
path is carried into parse errors to make them locatable.
}
\examples{
path <- tempfile(fileext = ".yml")
yaml_write(list(host = "localhost", port = 8080L), path)
yaml_read(path)
unlink(path)
}
